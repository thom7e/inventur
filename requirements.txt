# Kern-Abhängigkeiten (Laufzeit)
Flask>=3.0,<4.0
openpyxl>=3.1,<4.0

# Werkzeug wird von Flask mitgezogen, aber explizit gelistet
# weil werkzeug.security direkt verwendet wird
Werkzeug>=3.0,<4.0

# Optionale Skripte unter scripts/ (nicht für die Web-App erforderlich)
pandas>=2.2,<3.0
